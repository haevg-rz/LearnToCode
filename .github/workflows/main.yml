name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build a solutions
      run: Get-ChildItem -Path . -Include *.sln -Recurse | %{ dotnet build $_.FullName }
